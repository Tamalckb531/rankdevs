import LoginWrapper from "@/components/LoginWrapper";
import React from "react";

const page = () => {
  return (
    <div className=" h-[80vh]">
      <LoginWrapper />
    </div>
  );
};

export default page;
